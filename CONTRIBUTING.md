# 贡献指南

## 文档写作

文档面向需要完成具体任务的读者。每个段落优先说明以下内容：

1. 要完成什么任务。
2. 使用哪个入口、需要哪些输入。
3. 会得到什么返回值、表或文件。
4. 哪些限制会改变调用、集成或生产流程，以及调用方应采取什么动作。

能力描述必须能对应到公开 API、返回字段、示例或 benchmark。性能结论需要写明数据规模、参数和
运行环境，或链接到可复现的 benchmark。

不要使用“我们强调什么”“不是某个平台”“高保真”“可用于任何看板/Agent”等泛化表述。只有在限制
会影响读者的调用或集成方案时，才说明边界，并写出下一步动作。

Agent 相关说明只描述已有的结构化输入与调用方责任：MARS 可提供 report 表，调用方选择 Agent、
补充上下文并校验输出；不要暗示存在内置 Agent 功能。

改动 README、首页、用户指南或公开 docstring 时，人工检查是否回答了任务、输入、输出和必要限制。
不使用关键词黑名单，因为技术边界说明仍可能需要使用否定表达。
